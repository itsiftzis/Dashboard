package org.dashboard.dto;

public class Info {
    public Build getBuild() {
        return build;
    }

    public void setBuild(Build build) {
        this.build = build;
    }

    private Build build;
}
